# Document Authority Census Trigger

This temporary audit marker exists only to trigger a pull-request workflow run so the full-tree document-authority census artifact can be inspected through the GitHub connector.
