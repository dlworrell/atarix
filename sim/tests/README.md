# Simulator Tests

Simulator tests will validate virtual ATARIX node behavior under CI.

Initial planned tests:

```text
test_ping_pong
test_version_query
```

The tests must verify that traffic traverses the mailbox validator and does not bypass protocol validation.
